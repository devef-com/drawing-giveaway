services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: drawing-giveaway-app
    ports:
      - '3000:3000'
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
      - PGSSLMODE=disable
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - drawing-network

  db:
    image: postgres:16-alpine
    container_name: drawing-giveaway-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-drawing}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-drawing_password}
      POSTGRES_DB: ${POSTGRES_DB:-drawing_giveaway}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - '5432:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-drawing} -d ${POSTGRES_DB:-drawing_giveaway}']
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - drawing-network

volumes:
  postgres_data:

networks:
  drawing-network:
    driver: bridge
